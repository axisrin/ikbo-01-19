package com.company;


//Задача номер 1

import java.util.Collections;
import java.util.Scanner;

/*public class SortingByDirectInsertion {

    Student temp;
    String name;
    int id;
    Scanner scanner = new Scanner(System.in);

    public SortingByDirectInsertion() {

        Student[] studentsArray = new Student[8];
        for (int i = 0; i < studentsArray.length; i++) {
            name = scanner.next();
            id = scanner.nextInt();
            studentsArray[i] = new Student(id, name);
        }

        for (int i = 0; i < studentsArray.length; i++) {
            for (int j = i; j > 0; j--) {

                if ((studentsArray[j - 1].compareTo(studentsArray[j]) < 0)) {
                    temp = studentsArray[j - 1];
                    studentsArray[j - 1] = studentsArray[j];
                    studentsArray[j] = temp;
                }

            }
        }

        for (int i = 0; i < studentsArray.length; i++) {
            System.out.println("Student " + i + " " + "id = " + studentsArray[i].getiDNumber() + " Name : "
                    + studentsArray[i].getName());
        }

    }


}*/

